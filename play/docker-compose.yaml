services:
  postgres:
    image: 'postgres:16'
    environment:
      - POSTGRES_DB=play
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=123
    ports:
      - '5432:5432'
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
